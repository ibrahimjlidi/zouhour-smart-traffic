# MongoDB connection string (use full URI for production, e.g. Atlas)
MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@cluster0.s7eax9p.mongodb.net/smart_network_traffic
PORT=5000
JWT_SECRET=replace_this_with_a_secure_random_value
# Comma-separated list of allowed origins for CORS
# Example including Vercel frontend: https://zouhour-smart-traffic.vercel.app
ALLOWED_ORIGINS=http://localhost:5173,https://zouhour-smart-traffic.vercel.app
# Optional: set to 'true' to allow connecting with invalid TLS certs (debug only)
# ALLOW_TLS_INSECURE=false
