# Smart Network Traffic - Frontend

This is a minimal Vite + React + TailwindCSS frontend scaffold.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api/*` to `http://localhost:5000` so backend requests hit the existing server.
